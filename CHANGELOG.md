# Changelog

## v0.3.159+2009 (2026-09-16)

- 原生 B 站播放页新增 UP 主信息条：展示头像、名称与粉丝数，点击头像/名称先尝试唤起 B 站原生 App 的 `bilibili://space/<mid>`，失败回退网页版 `https://space.bilibili.com/<mid>`；右侧可直接关注/取消关注，取消关注前二次确认。
- 关注能力依赖后端新增 `GET /api/bilibili/user/card`（粉丝数与 `following` 状态）与 `POST /api/bilibili/user/follow`（后端持有 CSRF 调用 B 站关系接口）；旧版后端返回 404 时自动隐藏关注按钮，UP 主信息展示不受影响。
- 修复 iPhone 上打开评论输入框（键盘弹出）后播放器与下方 Tab 区挤压导致的 RenderFlex 溢出：键盘期间播放器按预算让出空间，键盘收起后自动恢复。
- 补充 UP 卡片 / 关注接口的单元与真实后端 E2E 覆盖，并用真实 B 站账号完成关注/取关往返及状态还原验证。

## v0.3.158+2008 (2026-09-14)

- 内置 WebView 播放页自动带上后端已有的 B 站登录态：优先复用 `play-url` 返回的 Cookie，否则调用一次 `auth/export`，网页版播放、弹幕与评论无需重新登录。Android 通过同源引导页写入原始 Cookie，避免 Cookie 值被二次 URL 编码；iOS/macOS 使用 `WKHTTPCookieStore` 直接写入，并规避 macOS 未实现背景色设置导致的异常。
- 新增随包离线分发的隐私政策与使用支持页，可直接在登录页和设置页打开，无需连接后端或登录。
- iOS 启动图改用应用图标替换默认占位图；补充 App Store 商店文案、隐私申报核对与备案准备等上架资料草稿。

## v0.3.157+2007 (2026-09-13)

- 「对话」Tab 顶部新增「对话标签红点」小开关：默认关闭，开启后在底部导航的「对话」图标上显示待聊积压总数红点，偏好持久化到本地并重启保持。
- 待聊红点数量改用后端返回的 `total` 完整去重积压总数（此前只统计当前页最多 10 条），与桌面 Web / 浏览器插件显示口径一致。

## v0.3.156+2006 (2026-09-11)

- 配套后端 `v0.3.221`：修复加入「稍后再看 / 收藏」时，B 站返回的协议相对封面（`//i2.hdslb.com/...`）被后端 422 拒绝的问题——归一化已在后端 API 边界统一完成，移动端无需改动即可正常保存。本次为对齐后端版本号的常规发版。

## v0.3.155+2005 (2026-09-10)

- 原生 B 站播放页的简介、点赞/投币/收藏/评论数改为优先通过后端 `/api/bilibili/video/info` 获取；后端使用 WBI 签名 `/x/web-interface/wbi/view` 回退，解决普通 `/view` 接口 412 导致播放页互动数据缺失的问题。

## v0.3.154+2004 (2026-09-09)

- 修复 Android versionCode 单调性问题：发版 build number 回退会导致已安装更高 versionCode 的设备无法覆盖安装（`INSTALL_FAILED_VERSION_DOWNGRADE`）。本次将版本提升到 `0.3.154+2004`，后续 CI 也会校验 build number 必须大于上一个已发布版本。

## v0.3.153+1 (2026-09-09)

- 换批、追加、下拉刷新的卡片与库存同次通知页面，顶部总数和来源 Tab 共用库存；缺库存字段时立即补读，失败明确显示「待同步」，避免旧数字滞留。新增首帧 UI 与乱序回归验证。

- 修复原生 B 站播放器评论时间把格式化函数打印成 `Closure` 的问题；主评论及复用该组件的完整回复楼正常显示日期，内嵌回复保持原有显示。
- 修复换批后总库存、各来源余量不同步及旧查询覆盖新状态；接入后端 `pool_status`。
- 修复零库存阻断手动重试、刷新失败错误标记耗尽、换批与追加并发覆盖。
- 底部自动续页保留用户意图，在冷却到点、请求结束及补货后重检；请求超时在 API 层统一为 12 秒并关闭连接。
- 新增推荐状态与定时器回归测试；完整验证范围见 `docs/recommendation-consistency.md`。

- 新增推荐真实 HTTP/WebSocket 交互集成测试及无线设备 driver。模拟器实际换批和惯性滚动追加两轮通过；实体 iPhone 无线调试未连接，保留未完成状态，详见推荐一致性文档。
