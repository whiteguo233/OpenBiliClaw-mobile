name: Release Android and iOS
run-name: Release ${{ inputs.release_tag || github.ref_name }}

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release 标签；留空时使用 pubspec.yaml 中的版本'
        required: false
        type: string
      prerelease:
        description: '标记为预发布版本'
        required: true
        default: false
        type: boolean
      publish_release:
        description: '创建 GitHub Release；关闭时只构建并上传 Actions Artifacts'
        required: true
        default: false
        type: boolean

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

env:
  FLUTTER_VERSION: '3.41.9'
  JAVA_VERSION: '17'

jobs:
  prepare:
    name: Validate release metadata
    runs-on: ubuntu-latest
    outputs:
      version_name: ${{ steps.version.outputs.version_name }}
      build_number: ${{ steps.version.outputs.build_number }}
      release_tag: ${{ steps.version.outputs.release_tag }}
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Validate version and source branch
        id: version
        shell: bash
        env:
          INPUT_RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          set -euo pipefail

          if [[ "$GITHUB_EVENT_NAME" == "workflow_dispatch" && "$GITHUB_REF" != "refs/heads/main" ]]; then
            echo "Manual releases must run from the main branch." >&2
            exit 1
          fi

          VERSION_FULL="$(awk '/^version:/{print $2; exit}' pubspec.yaml)"
          if [[ "$VERSION_FULL" =~ ^([0-9]+\.[0-9]+\.[0-9]+)\+([0-9]+)$ ]]; then
            VERSION_NAME="${BASH_REMATCH[1]}"
            BUILD_NUMBER="${BASH_REMATCH[2]}"
          else
            echo "pubspec.yaml version must use X.Y.Z+BUILD format; got: $VERSION_FULL" >&2
            exit 1
          fi

          if [[ "$GITHUB_EVENT_NAME" == "push" ]]; then
            RELEASE_TAG="$GITHUB_REF_NAME"
          else
            RELEASE_TAG="${INPUT_RELEASE_TAG:-v$VERSION_NAME}"
          fi

          if [[ "$RELEASE_TAG" != "v$VERSION_NAME" ]]; then
            echo "Release tag $RELEASE_TAG does not match pubspec version v$VERSION_NAME." >&2
            exit 1
          fi

          git fetch origin main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "Release commit must be contained in origin/main." >&2
            exit 1
          fi

          echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "build_number=$BUILD_NUMBER" >> "$GITHUB_OUTPUT"
          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"

          {
            echo "### Release metadata"
            echo "- Tag: \`$RELEASE_TAG\`"
            echo "- App version: \`$VERSION_NAME+$BUILD_NUMBER\`"
            echo "- Commit: \`$GITHUB_SHA\`"
          } >> "$GITHUB_STEP_SUMMARY"

  quality:
    name: Analyze and test
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Resolve dependencies
        run: flutter pub get

      - name: Verify locked dependencies
        run: git diff --exit-code -- pubspec.lock

      - name: Check formatting
        run: dart format --output=none --set-exit-if-changed lib test

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

  android:
    name: Build signed Android artifacts
    needs: [prepare, quality]
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
      ANDROID_STORE_PASSWORD: ${{ secrets.ANDROID_STORE_PASSWORD }}
      OPENBILICLAW_REQUIRE_RELEASE_SIGNING: 'true'
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Configure Android signing path
        shell: bash
        run: echo "ANDROID_KEYSTORE_PATH=$RUNNER_TEMP/openbiliclaw-release.jks" >> "$GITHUB_ENV"

      - name: Set up Java
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4.0.1

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Validate Android signing secrets
        shell: bash
        run: |
          set -euo pipefail
          missing=()
          for variable in ANDROID_KEYSTORE_BASE64 ANDROID_KEY_ALIAS ANDROID_KEY_PASSWORD ANDROID_STORE_PASSWORD; do
            if [[ -z "${!variable:-}" ]]; then
              missing+=("$variable")
            fi
          done
          if (( ${#missing[@]} > 0 )); then
            echo "Missing Android release secrets: ${missing[*]}" >&2
            exit 1
          fi

      - name: Install and verify Android signing key
        shell: bash
        run: |
          set -euo pipefail
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | openssl base64 -d -A -out "$ANDROID_KEYSTORE_PATH"
          keytool -list \
            -keystore "$ANDROID_KEYSTORE_PATH" \
            -storepass "$ANDROID_STORE_PASSWORD" \
            -alias "$ANDROID_KEY_ALIAS" >/dev/null

      - name: Resolve dependencies
        run: flutter pub get

      - name: Build signed split APKs
        run: >-
          flutter build apk --release --split-per-abi
          --target-platform android-arm,android-arm64
          --obfuscate
          --split-debug-info=build/symbols/android/apk
          --build-name=${{ needs.prepare.outputs.version_name }}
          --build-number=${{ needs.prepare.outputs.build_number }}

      - name: Build signed App Bundle
        run: >-
          flutter build appbundle --release
          --obfuscate
          --split-debug-info=build/symbols/android/aab
          --build-name=${{ needs.prepare.outputs.version_name }}
          --build-number=${{ needs.prepare.outputs.build_number }}

      - name: Package Android artifacts
        shell: bash
        env:
          VERSION_NAME: ${{ needs.prepare.outputs.version_name }}
        run: |
          set -euo pipefail
          mkdir -p dist/android
          cp build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk \
            "dist/android/OpenBiliClaw-Android-armeabi-v7a-v$VERSION_NAME.apk"
          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
            "dist/android/OpenBiliClaw-Android-arm64-v8a-v$VERSION_NAME.apk"
          cp build/app/outputs/bundle/release/app-release.aab \
            "dist/android/OpenBiliClaw-Android-v$VERSION_NAME.aab"
          (
            cd build/symbols/android
            zip -qry "$GITHUB_WORKSPACE/dist/android/OpenBiliClaw-Android-symbols-v$VERSION_NAME.zip" .
          )

      - name: Upload Android artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: openbiliclaw-android-v${{ needs.prepare.outputs.version_name }}
          path: dist/android/*
          if-no-files-found: error
          retention-days: 14

      - name: Remove Android signing key
        if: always()
        run: rm -f "$ANDROID_KEYSTORE_PATH"

  ios:
    name: Build unsigned iOS IPA
    needs: [prepare, quality]
    runs-on: macos-latest
    timeout-minutes: 45
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Resolve dependencies
        run: flutter pub get

      - name: Build unsigned iOS app
        run: >-
          flutter build ios --release --no-codesign
          --obfuscate
          --split-debug-info=build/symbols/ios
          --build-name=${{ needs.prepare.outputs.version_name }}
          --build-number=${{ needs.prepare.outputs.build_number }}

      - name: Package unsigned IPA and symbols
        shell: bash
        env:
          VERSION_NAME: ${{ needs.prepare.outputs.version_name }}
        run: |
          set -euo pipefail
          APP_PATH="build/ios/iphoneos/Runner.app"
          IPA_ROOT="$RUNNER_TEMP/openbiliclaw-unsigned-ipa"
          IPA_PATH="$GITHUB_WORKSPACE/dist/ios/OpenBiliClaw-iOS-unsigned-v$VERSION_NAME.ipa"

          if [[ ! -d "$APP_PATH" ]]; then
            echo "Unsigned Runner.app was not produced." >&2
            exit 1
          fi
          if codesign --verify "$APP_PATH" >/dev/null 2>&1; then
            echo "Expected an unsigned iOS app, but a valid signature was found." >&2
            exit 1
          fi

          mkdir -p dist/ios
          mkdir -p "$IPA_ROOT/Payload"
          ditto --noextattr --norsrc "$APP_PATH" "$IPA_ROOT/Payload/Runner.app"

          SIGNATURE_INDEX=0
          while IFS= read -r -d '' SIGNATURE_PATH; do
            SIGNATURE_INDEX=$((SIGNATURE_INDEX + 1))
            mv "$SIGNATURE_PATH" "$RUNNER_TEMP/openbiliclaw-stripped-signature-$SIGNATURE_INDEX"
          done < <(find "$IPA_ROOT/Payload/Runner.app" -type d -name _CodeSignature -prune -print0)
          if [[ -f "$IPA_ROOT/Payload/Runner.app/embedded.mobileprovision" ]]; then
            mv "$IPA_ROOT/Payload/Runner.app/embedded.mobileprovision" \
              "$RUNNER_TEMP/openbiliclaw-stripped.mobileprovision"
          fi

          ditto -c -k --keepParent "$IPA_ROOT/Payload" "$IPA_PATH"
          IPA_LISTING="$(unzip -Z1 "$IPA_PATH")"
          if ! grep -Fxq 'Payload/Runner.app/Info.plist' <<< "$IPA_LISTING"; then
            echo "Unsigned IPA does not contain Payload/Runner.app/Info.plist." >&2
            exit 1
          fi
          if grep -q '_CodeSignature' <<< "$IPA_LISTING"; then
            echo "Unsigned IPA unexpectedly contains _CodeSignature." >&2
            exit 1
          fi
          if grep -q '^__MACOSX/' <<< "$IPA_LISTING"; then
            echo "Unsigned IPA unexpectedly contains __MACOSX metadata." >&2
            exit 1
          fi

          if [[ -d build/ios/Release-iphoneos/Runner.app.dSYM ]]; then
            ditto -c -k --sequesterRsrc --keepParent \
              build/ios/Release-iphoneos/Runner.app.dSYM \
              "dist/ios/OpenBiliClaw-iOS-dSYM-v$VERSION_NAME.zip"
          fi
          ditto -c -k --keepParent \
            build/symbols/ios \
            "dist/ios/OpenBiliClaw-iOS-Flutter-symbols-v$VERSION_NAME.zip"
          cp .github/IOS_SELF_SIGNING.md dist/ios/OpenBiliClaw-iOS-SELF-SIGNING.md

      - name: Upload iOS artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: openbiliclaw-ios-v${{ needs.prepare.outputs.version_name }}
          path: dist/ios/*
          if-no-files-found: error
          retention-days: 14

  release:
    name: Publish GitHub Release
    needs: [prepare, android, ios]
    if: github.event_name == 'push' || inputs.publish_release
    runs-on: ubuntu-latest
    environment: github-release
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: openbiliclaw-*-v${{ needs.prepare.outputs.version_name }}
          path: artifacts
          merge-multiple: true

      - name: Generate SHA-256 checksums
        shell: bash
        env:
          VERSION_NAME: ${{ needs.prepare.outputs.version_name }}
        run: |
          set -euo pipefail
          cd artifacts
          find . -maxdepth 1 -type f ! -name '*-SHA256SUMS.txt' -printf '%f\0' \
            | sort -z \
            | xargs -0 sha256sum \
            > "OpenBiliClaw-v$VERSION_NAME-SHA256SUMS.txt"

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: artifacts/*

      - name: Publish release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          tag_name: ${{ needs.prepare.outputs.release_tag }}
          target_commitish: ${{ github.sha }}
          name: OpenBiliClaw ${{ needs.prepare.outputs.release_tag }}
          generate_release_notes: true
          prerelease: ${{ github.event_name == 'workflow_dispatch' && inputs.prerelease }}
          fail_on_unmatched_files: true
          make_latest: true
          files: artifacts/*
